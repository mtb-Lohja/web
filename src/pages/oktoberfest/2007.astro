---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

// Get all posts for Oktoberfest 2007
const allPosts = await getCollection('posts');
const oktoberfest2007Posts = allPosts
  .filter(post => post.data.path.startsWith('/oktoberfest/2007'))
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<BaseLayout title="MTB-Lohja toy - Oktoberfest 2007">
  <div class="post-container">
    <article class="post">
      <header>
        <h2>Oktoberfest 2007</h2>
      </header>
      <div class="post-content">
        <p>MTB-Lohjan Oktoberfest-tapahtuma vuodelta 2007.</p>
        <ul>
          {oktoberfest2007Posts.map(post => (
            <li>
              <a href={post.data.path}>
                <strong>{post.data.title}</strong>
              </a>
              <span class="quiet"> - {post.data.date.toLocaleDateString('fi-FI')}</span>
            </li>
          ))}
        </ul>
      </div>
    </article>
  </div>
</BaseLayout>